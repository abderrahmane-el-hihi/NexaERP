import { createTestTenant } from "./tests/helpers/factory"; import { checkAllTenants } from "./src/modules/platform/services/invariant-checker.service"; async function run() { console.log("Seeding tenant..."); const t = await createTestTenant(); console.log("Tenant created:", t.tenantId); console.log("Running invariants..."); const r = await checkAllTenants(); console.log(JSON.stringify(r, null, 2)); } run().catch(console.error);
